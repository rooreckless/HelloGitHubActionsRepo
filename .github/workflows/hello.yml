name: Hello, World!
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - run: |
          pwd
      - run: |
          ls -l
      - run: |
          pip install -U pip
          pip install -r ../requirements.txt
